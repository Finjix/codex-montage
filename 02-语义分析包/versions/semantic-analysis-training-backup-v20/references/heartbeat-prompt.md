# Watchdog-only heartbeat prompt for a V18 production task

Recover the V18 production job represented by the task-local pipeline config and orchestrator state. First run `v15_orchestrator.py watchdog`. If it reports `foreground_active` or `recent_progress_no_recovery`, do nothing and stay quiet. Never run a production slice while a fresh foreground lease exists. If it returns `recovery_required`, call `run-continuous --recovery` with the configured bounded recovery budget. If it returns `model_required`, perform only the named Terra or Sol phase, write `semantic-model-phase-response/v18`, call `supply-model`, then run one bounded recovery slice. The heartbeat is not allowed to use normal unbounded foreground mode. Preserve passed rolling groups and never deliver rejects. For `celebrity_live_action_opening/v14`, preserve the first-fullscreen-game boundary and never admit gameplay, the game tail, end cards or CTA. Apply the V18 content-grounding and advertising-close contract on every recovered plan. Notify only on completion, exhausted retries/repair rounds, true capacity/authority conflict, or unrecoverable no-progress failure. Do not call external models without a separate current-user request.
