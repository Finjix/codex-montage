# V20.2.4 全文件夹问题总结与门禁升级

范围：`V20.2.1新40条` 文件夹从初版到最终修复期间暴露的全部问题，不限于最后一次 S08 头音。

## 问题归类

1. **旧坏素材复用**：用户已经判坏的切点被改名、做成清洁中间文件或换 candidate ID 后再次使用；只修点名视频，没有按共享候选查完依赖成片。
2. **夹帧、碎镜和冻结**：异镜头残留、手指/摆手废帧、30–59帧碎镜、挥手卡住、相邻同源分段拼接导致重复或跳帧。
3. **夹音、气口和瞬态**：`嗨/额/呃/啊/噔/咔/8/一` 等前后句残音被带入；机器波形有能量就误判为人物开口。
4. **半句话和尾字缺失**：结尾“器”没读完、礼包码或 CTA 被截断、`现在玩无尽冬日` 只留半句、过短口号凑结尾。
5. **语义断链**：问答错位、打假后直接跳玩法、砍树/熔炉/幸存者因果倒置、第一人称和客观叙述主体跳变、机制链中插入福利再跳回结果。
6. **人物与画面语义**：群演单独出现、明星身份开头没有第一人称经历承接、文字能接但画面和语气不接。
7. **CTA/福利位置**：完整 CTA 和礼包码曾被排除；后来又出现福利突然插在中段、未与具体礼包内容相连。
8. **结尾功能错误**：幸存者数量、方法句、短口号或同义口号堆叠做结尾，缺少结果、收益、证明或行动闭环。
9. **时长与镜头失衡**：继承历史 23–26 秒并为了时长补素材；首镜头过短、尾镜头过长；用停帧或无效内容补时长。
10. **结构重复**：六条完全相同片头、开头+第二段重复、只换片头或片尾而中段顺序长期不变。
11. **终检漏检**：旧 ASR/旧通过记录复用；只看文本或技术解码，没有同时检查首词、语气、画面动作、PCM 和完整话轮。
12. **长任务恢复**：闪退时逐帧证据没有正式回执，若没有任务检查点容易误把半成品当成已完成。

## 从源头新增的硬门禁

- 派生/清洁文件必须提供 `source-lineage-evidence/v20.4`，追溯原片 SHA-256 与原始帧区间。
- 失效区间同时匹配当前文件、原片血缘和嵌入式内容指纹；重命名或换中间文件不能洗白。
- 语音入口只认预期文本强制对齐；首个预期词前最多3帧，禁止用波纹或能量阈值代替。
- 每个候选必须是完整话轮；首词、尾字、语气和动作同时闭合。
- 任一坏候选撤销全部依赖计划、相邻转场、路线签名和旧终检。
- 默认按60帧复查入口、出口和拼接点；两侧各提取至少72帧。
- 1–29帧异镜头直接拒绝；30–59帧碎镜默认拒绝。
- 相邻同原片、同速度、帧区间单调相接/重叠时，先合并为连续原片区间再渲染。
- 最终回执升级为 `ffmpeg-post-encode-qc/v20.5`：fresh ASR、预期首词、前句尾词、完整话轮、残音/瞬态全部必须通过。
- 时长只读当前工作单；禁止继承历史时长和为时长补无效素材。
- 同一实际首镜头视觉族硬上限3次，不存在第4次例外；候选名、文件名或台词不同不能把同一画面算成新片头。开头+第二段视觉组合最多2次；40条计划锁定前必须证明至少14个可用片头视觉族。
- CTA、礼包码和福利必须完整、与当前命题相连，通常放在片尾。
- 所有正式制作和返修必须经过 FF 三件套状态机；临时脚本和手工替换不能生成发布回执。
- 完整话轮内部纯气口允许在用户授权后按原生帧压缩，但删除区必须零词/音素交集，并保护前词尾音、后词最多3帧余量和连接词前提。
- PCM满幅峰值只能用同一帧区间的均匀降幅派生源修复，禁止静音或删音遮盖。
- 最终发布必须重新检查编码后每条开头至少60帧的实际视觉族；计划标签不能代替最终证据。

## 本次验证

- 语义包版本：`20.2.5`
- 控制器版本：`1.5.0`
- 执行器版本：`1.5.0`
- 相关语义、血缘、逐帧、首词和后编码回归测试：通过
- 语义包成员哈希：98/98 通过
- 外部三件套组件树、运行时锁和预检：全部通过
- 外部模型/OpenCode：未调用
