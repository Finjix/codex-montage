# V11 continuous rolling production

Normal production runs `scripts/v11_orchestrator.py run-continuous`. Exit 22 immediately continues the next durable slice in the same active Codex turn. The registered heartbeat is recovery-only.

V11 retains V10's global plan witness and rolling groups of five. Passing indexes are frozen; rejects are quarantined and repaired without blocking later clean groups. Completion requires every requested index and `semantic-delivery-manifest/v11`.

The work order must select one explicit style profile. `historical_low_repeat_rolling/v10` remains available for the accepted generic 25--30 second montage. `celebrity_live_action_opening/v11` is separate: it ends at the first dominant full-screen gameplay frame and requires the V11 structured live-action, CTA, identity, transition and speaker-return evidence.

For witness-sufficient inventory, use a task-specific witness-scope source manifest instead of passing the entire source library as an artificial requirement. The global witness must still prove every requested plan and the replacement margin. Expand the scope and rerun candidate review when that proof fails; never relabel unprocessed sources exhausted.
